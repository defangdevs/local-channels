{
  "$schema": "https://anthropic.com/claude-code/marketplace.schema.json",
  "name": "local-channels",
  "description": "Local channel plugins for this host",
  "owner": {
    "name": "claude"
  },
  "plugins": [
    {
      "name": "local-webhook",
      "description": "HMAC-verified webhook channel: pushes deliveries from GitHub or any other signing sender into the session, plus webhook_subscribe / webhook_unsubscribe MCP tools (and an equivalent webhook.py CLI) for topic routing — including deliver_to:\"subagent\" standing watches that spawn a fresh session per event batch instead of interrupting the active one.",
      "source": "./local-webhook",
      "keywords": ["channel", "mcp", "webhook", "github"]
    }
  ]
}
