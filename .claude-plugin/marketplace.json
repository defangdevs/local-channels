{
  "$schema": "https://anthropic.com/claude-code/marketplace.schema.json",
  "name": "local-channels",
  "description": "Local channel plugins for this host",
  "owner": {
    "name": "claude"
  },
  "plugins": [
    {
      "name": "gh-webhook",
      "description": "HMAC-verified GitHub webhook channel that pushes repo events into the session, plus repo_subscribe / repo_unsubscribe MCP tools for routing which repos come through.",
      "source": "./gh-webhook",
      "keywords": ["channel", "mcp", "github", "webhook"]
    }
  ]
}
